

lexer section:

    # test top-level sequence exprs, and sequence exprs in group exprs

    # also test w/ and w/out nested sequence subexprs

    LPR0        : any any | any | ( any any | any ) ;

parser section:

    # test top-level sequence exprs, and sequence exprs in group exprs
    
    # also test w/ and w/out nested sequence subexprs

    PPR0        : any any | any | ( any any | any ) ;

