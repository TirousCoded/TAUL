

lexer section:

    # test top-level sequence exprs, and sequence exprs in group exprs

    LPR0        : any ( any any ) any ;

parser section:

    # test top-level sequence exprs, and sequence exprs in group exprs

    PPR0        : any ( any any ) any ;

